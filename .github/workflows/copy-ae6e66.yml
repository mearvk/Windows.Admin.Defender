name: Copy AE6E66 module

on:
  push:
    paths:
      - .github/workflows/copy-ae6e66.yml

permissions:
  contents: write

jobs:
  copy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout destination repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: Copy AE6E66 exactly from source repository
        shell: bash
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT
          git clone --depth 1 --branch main https://github.com/mearvk/Java.Web.Server.Telnet.Front.Java.21.git "$tmp/source"
          rm -rf modules/AE6E66
          mkdir -p modules
          cp -a "$tmp/source/modules/AE6E66" modules/AE6E66

      - name: Commit copied module and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/workflows/copy-ae6e66.yml
          git add modules/AE6E66
          git diff --cached --quiet && { echo "No changes to copy."; exit 0; }
          git commit -m "Copy AE6E66 module"
          git push
