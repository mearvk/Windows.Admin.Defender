<?xml version="1.0" encoding="utf-8"?>
<!--
  WindowsAdminDefender.vcxproj
  WDK file-system minifilter (kernel-mode) project for source/WindowsAdminDefender.c.

  The WDK/SDK are supplied via NuGet packages imported by the solution-level
  Directory.Build.props (restored under source/packages). This is Microsoft's
  supported CI/CD path and avoids requiring a machine-wide WDK Visual Studio
  extension. It does not disable driver-signing policy, Secure Boot, Defender,
  Device Guard, or UAC. Catalog generation and signing remain a separate,
  explicit packaging step.
-->
<Project DefaultTargets="Build" ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Debug|ARM64">
      <Configuration>Debug</Configuration>
      <Platform>ARM64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|ARM64">
      <Configuration>Release</Configuration>
      <Platform>ARM64</Platform>
    </ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <ProjectGuid>{6F2D8B41-2C7A-4E19-9B33-3C1A5E7D9A01}</ProjectGuid>
    <TemplateGuid>{497e31cb-01e3-4e64-8f61-1eee3e8d2f77}</TemplateGuid>
    <TargetFrameworkVersion>v4.5</TargetFrameworkVersion>
    <MinimumVisualStudioVersion>16.0</MinimumVisualStudioVersion>
    <Configuration>Release</Configuration>
    <Platform Condition="'$(Platform)' == ''">x64</Platform>
    <RootNamespace>WindowsAdminDefender</RootNamespace>
    <DriverType>WDM</DriverType>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <PropertyGroup Label="Configuration">
    <TargetVersion>Windows10</TargetVersion>
    <UseDebugLibraries Condition="'$(Configuration)' == 'Debug'">true</UseDebugLibraries>
    <UseDebugLibraries Condition="'$(Configuration)' == 'Release'">false</UseDebugLibraries>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
    <ConfigurationType>Driver</ConfigurationType>
    <DriverType>WDM</DriverType>
    <DriverTargetPlatform>Universal</DriverTargetPlatform>
    <!--
      CI validates that the driver COMPILES and LINKS. Test-signing and INF
      verification are packaging steps that need signing certs / tools not
      present on a hosted runner (recent WDKs also dropped the 32-bit
      InfVerif.dll). They are disabled here so the build produces the .sys;
      production packaging performs signing and INF verification separately.
    -->
    <SignMode>Off</SignMode>
    <InfVerif_EnableVerification>false</InfVerif_EnableVerification>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ImportGroup Label="PropertySheets" />

  <PropertyGroup>
    <TargetName>WindowsAdminDefender</TargetName>
  </PropertyGroup>

  <ItemDefinitionGroup>
    <ClCompile>
      <WppEnabled>false</WppEnabled>
      <TreatWarningAsError>true</TreatWarningAsError>
      <WarningLevel>Level4</WarningLevel>
      <PreprocessorDefinitions>POOL_NX_OPTIN=1;%(PreprocessorDefinitions)</PreprocessorDefinitions>
    </ClCompile>
    <ClCompile Condition="'$(Configuration)' == 'Release'">
      <Optimization>MinSpace</Optimization>
    </ClCompile>
    <Link>
      <!-- Filter Manager import library for the minifilter APIs used in DriverEntry. -->
      <AdditionalDependencies>fltMgr.lib;%(AdditionalDependencies)</AdditionalDependencies>
    </Link>
    <Inf>
      <SpecifyDriverVerDirectiveVersion>true</SpecifyDriverVerDirectiveVersion>
    </Inf>
  </ItemDefinitionGroup>

  <ItemGroup>
    <ClCompile Include="WindowsAdminDefender.c" />
  </ItemGroup>
  <ItemGroup>
    <ClInclude Include="WindowsAdminDefender.h" />
  </ItemGroup>
  <ItemGroup>
    <Inf Include="WindowsAdminDefender.inf" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
